name: lab01
on: [push]
jobs: 
    test: 
        runs-on: ubuntu-latest
        steps: 
            - run: pip install nbconvert
            - run: sudo apt-get install pandoc inkscape texlive-xetex
            - uses: actions/checkout@v4
            - run: python -m nbconvert --to pdf lab1/lab1.ipynb